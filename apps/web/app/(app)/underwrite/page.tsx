import { PagePlaceholder } from '@/components/page-placeholder'

export default function UnderwritePage() {
  return (
    <PagePlaceholder
      title="Underwrite"
      subtitle="Market-maker rail — stream firm quotes, beat the pool load, post collateral to the UnderwriterVault."
    />
  )
}
