import { PagePlaceholder } from '@/components/page-placeholder'

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      subtitle="Your positions across every role — protections, deposits, MM fills — and settle actions."
    />
  )
}
