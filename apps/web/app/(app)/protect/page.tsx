import { PagePlaceholder } from '@/components/page-placeholder'

export default function ProtectPage() {
  return (
    <PagePlaceholder
      title="Protect"
      subtitle="Buy IL protection on your Uniswap v3 position — covered while in range, capped at MaxIL."
    />
  )
}
