# WalletConnect Cloud project id (RainbowKit). Required for WC features at runtime.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Arbitrum Sepolia RPC the dApp reads from.
NEXT_PUBLIC_RPC_URL=

# Backend (task #30 — set once the API/engine are hosted):
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_ENGINE_WS_URL=
