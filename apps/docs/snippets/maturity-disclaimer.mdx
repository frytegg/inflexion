<Note>
**Structures ship from day one; the full DYNAMICS (pool-vs-MM spread as a forward-vol signal) mature with multiple competing MMs and volume. The dataset begins at the single redeploy; the off-chain telemetry halves begin at the first interaction logged.**

What Inflexion offers today is the **architecture and the first public view** into the microstructure of the DeFi LP volatility-risk premium — not yet a mature dataset:

- **Structures are present day-one.** All five non-circular behavioral signals are defined and queryable from launch.
- **Dynamics mature with volume.** The richer signal — the pool-vs-MM spread read as a forward-vol indicator — only sharpens once multiple market makers compete and real flow accumulates.
- **The dataset begins at the single redeploy**, and the off-chain telemetry halves begin at the first interaction logged. Until the subgraph is deployed, history degrades to a typed `pending` state — never an error.
</Note>
