{
  "_README": [
    "PROPOSED cvAMM block schema for quant/params.json. DOCUMENTATION ONLY.",
    "Do NOT add this block to params.json or params.py THIS TURN. params.json is",
    "pydantic extra='forbid' at schema_version 2.0.0 and is byte-identical-roundtrip",
    "tested; params.py is its pydantic model. Adding the block now would break CI.",
    "In P1: (1) add a `CvammParams` pydantic model to params.py, (2) attach it under",
    "params.json key `cvamm`, (3) bump schema_version MINOR (2.0.0 -> 2.1.0; a field",
    "is added, old consumers tolerate it), (4) fill every TODO from the calibration run.",
    "EVERY value below marked TODO_PLACEHOLDER is a placeholder, NOT a calibrated number.",
    "No contract may hardcode any of these; all are read from params.json (CLAUDE.md inv. 6).",
    "See quant/SPEC.md for the work order behind each field and quant/README.md for context."
  ],
  "schema_version_target": "2.1.0",
  "schema_version_note": "minor bump over the frozen 2.0.0 — adds the `cvamm` key, does not remove/rename existing PARTIAL fields",
  "cvamm": {
    "_doc": "All primitives the on-chain FairValueOracle + ConvexityVault (cvAMM) read. Single-asset ETH/USDC launch, FULL mode.",

    "markets": {
      "_doc": "9 launch marketIds: ETH/USDC, 3 fee tiers x 3 durations. marketId = keccak(token0,token1,fee,durationSeconds).",
      "pair": "ETH/USDC",
      "fee_tiers_bps": [500, 3000, 10000],
      "durations_days": [7, 30, 90],
      "n_markets": 9
    },

    "fair_premium_surface": {
      "_doc": "Deliverable 1. fairRate = E_Q[min(IL,MaxIL)]/MaxIL, FairPremium = fairRate*MaxIL. EXACT CLOSED FORM (finite Phi-sum over the piecewise v3 payoff) — NOT an MC/fitted/lookup surface, NO calibrated coefficients. Evaluated live on-chain from (Pa,Pb,L,sigma_ref,T). MaxIL is pure geometry, computed on-chain by ILMath, not stored here.",
      "measure": "risk_neutral_gbm_mu0_r0",
      "representation": "exact_closed_form_phi_sum",
      "coefficients": "none — the closed form has NO calibrated coefficients; the only stochastic input is sigma_ref (deliverable 2)",
      "calibration_sweep_dims": {
        "_doc": "domain over which the LOAD/SKEW deliverables (3-6) are calibrated by CALLING the exact closed form — NOT a grid the on-chain pricer stores",
        "width_frac": "e.g. [0.05, 0.10, 0.20, 0.50]",
        "distance_to_edge_frac": "e.g. [0.0, 0.25, 0.5, 0.75, 1.0] (position of P0 within [Pa,Pb])",
        "T_days": [7, 30, 90],
        "sigma": "e.g. [0.30, 0.45, 0.60, 0.90, 1.50]"
      },
      "verification": {
        "_doc": "VERIFIED this pivot: closed form == il.py quadrature == MC to ~5e-11 across width x sigma x T (quant/_scratch_fairvalue_closedform_check.py). Re-confirm in-repo before wiring on-chain.",
        "max_abs_err_vs_il_py": 5.1e-11,
        "theory_anchors_not_the_pricer": "Singh et al. AFT 2025 (LVR == theta of ATM straddle) + Lipton-Lucic-Sepp 2025 (static replication by a vanilla strip) explain WHY the claim is priceable/hedgeable; they are anchors, cited not re-derived, NOT the on-chain pricer (the exact Phi-sum is)."
      },
      "_reference_points_sigma60": {
        "_doc": "il.py ground truth; the on-chain pricer uses the position's real tick-derived (Pa,Pb), so the +/-width convention is a reference-table convention only",
        "geometric_halfwidth": {
          "w5": { "d7": 0.695, "d30": 0.848, "d90": 0.913 },
          "w10": { "d7": 0.449, "d30": 0.708, "d90": 0.829 },
          "w20": { "d7": 0.182, "d30": 0.473, "d90": 0.674 }
        },
        "arithmetic_halfwidth": {
          "w10": { "d7": 0.4288, "d30": 0.6942, "d90": 0.8206 },
          "w5_d30": 0.8453,
          "w20_d30": 0.4312,
          "w35_d90": 0.4053
        }
      }
    },

    "sigma_ref": {
      "_doc": "Deliverable 2. sigma_ref = max(sigma_short, sigma_long, floor), EWMA of log-returns from Chainlink ticks. NEVER price off raw realized sigma. Deribit DVOL = optional enrichment only, never depended on.",
      "short_halflife_seconds": "TODO_PLACEHOLDER: e.g. 86400 (1d)",
      "long_halflife_seconds": "TODO_PLACEHOLDER: e.g. 2592000 (30d)",
      "floor": "TODO_PLACEHOLDER: annualized sigma floor, e.g. 0.40",
      "estimator": "ewma_log_returns_max_short_long_floor",
      "conservative_caveat": "max(...) is mandatory: realized vol understates risk before a regime change"
    },

    "base_load_by_regime": {
      "_doc": "Deliverable 3. baseLoad = vol-risk-premium, keyed by sigma_ref regime band. Sized inside the lone-writer-CVaR (~91-100% MaxIL) vs diversified-N-CVaR (~78.7% at N=100) envelope.",
      "regime_bands_sigma": "TODO_PLACEHOLDER: e.g. {calm: <0.45, normal: 0.45-0.90, stressed: >0.90}",
      "base_load_bps": {
        "calm": "TODO_PLACEHOLDER",
        "normal": "TODO_PLACEHOLDER",
        "stressed": "TODO_PLACEHOLDER"
      }
    },

    "util_skew": {
      "_doc": "Deliverable 4. util_skew(u), u = locked/(locked+free). Rises near full commitment; wires into the withdrawal-delay/locked-free defense. Single-asset (NOT the dead cross-asset k~1.0).",
      "argument": "locked_over_locked_plus_free",
      "form": "TODO_PLACEHOLDER: e.g. monotone convex (knee + slope), or piecewise-linear",
      "params": "TODO_PLACEHOLDER"
    },

    "dispersion_skew": {
      "_doc": "Deliverable 5. Rises as outstanding coverage clusters in one width/moneyness/duration corner — the single-pair analogue of concentration. The cross-asset k-skew degenerates to 1.0 on one pair; do NOT reuse it.",
      "concentration_metric": "TODO_PLACEHOLDER: e.g. Herfindahl index over coverage histogram across (width x moneyness x duration)",
      "form": "TODO_PLACEHOLDER: monotone in concentration",
      "params": "TODO_PLACEHOLDER"
    },

    "max_load_bps": {
      "_doc": "Deliverable 6. Invariant I10 cap: premium <= FairPremium*(1+maxLoadBps). baseLoad + util_skew + dispersion_skew clamped to this BY CONSTRUCTION. Applies to BOTH paths (Path B: quote loadBps <= maxLoadBps). UPSTREAM of settle; does NOT touch settle/MaxIL/I1-I9.",
      "by_width_x_duration": {
        "_doc": "keyed to the 9-marketId grid; tight/short tolerates a different cap than wide/long",
        "w5": { "d7": "TODO_PLACEHOLDER", "d30": "TODO_PLACEHOLDER", "d90": "TODO_PLACEHOLDER" },
        "w10": { "d7": "TODO_PLACEHOLDER", "d30": "TODO_PLACEHOLDER", "d90": "TODO_PLACEHOLDER" },
        "w20": { "d7": "TODO_PLACEHOLDER", "d30": "TODO_PLACEHOLDER", "d90": "TODO_PLACEHOLDER" }
      }
    },

    "productive_collateral": {
      "_doc": "Deliverable 7. ROADMAP. COMPLIANT FORM ONLY (CLAUDE.md + spec 7.2 F-#3): idle/free USDC only, instantly-redeemable wrappers only, never utilization-gated, hard cap, never 100%, keep nude buffer. 'Aave for locked collateral' is BLOCKED pending owner override — NOT encoded.",
      "max_idle_routable_frac": "TODO_PLACEHOLDER: e.g. 0.50 (of IDLE/FREE USDC only; locked never routed)",
      "venue_whitelist": ["sDAI", "tokenized_tbills"],
      "venue_blocklist_reason": "Aave/Compound and any utilization-gated venue are BLOCKED for ALL vault USDC (locked AND idle) per CLAUDE.md",
      "nude_buffer_frac": "TODO_PLACEHOLDER: e.g. 0.20 (nude USDC kept for worst-case simultaneous claims)",
      "status": "roadmap"
    },

    "pool_hedge_fraction": {
      "_doc": "Deliverable 8. ROADMAP. Fraction of aggregate tail convexity bought back (long option strip / long Panoptic). Changes the NATURE of the risk (halves worst month) at APY cost. Approximate (perpetual vs fixed-maturity gamma); NOT relied on for solvency.",
      "fraction": "TODO_PLACEHOLDER: e.g. 0.30 (optimal on the APY-vs-tail frontier)",
      "status": "roadmap"
    },

    "tranche_cut": {
      "_doc": "Deliverable 9. ROADMAP. Senior (hedged, base yield, low tail) / Junior (unhedged, first loss, high APY) cut point. No engineering makes a single-pair vol seller low-risk; tranching lets each depositor pick risk dose.",
      "senior_fraction": "TODO_PLACEHOLDER: e.g. 0.60 (senior share of pool capital)",
      "junior_first_loss": true,
      "status": "roadmap"
    },

    "disclosure": {
      "_doc": "Deliverable 10. REAL single-asset ETH depositor numbers. PLACEHOLDERS pending P1 single-asset calibration — the old multi-asset figures are materially optimistic for one pair. Verbatim tone mandatory; never 'stable/modest APY'.",
      "apy": "TODO_PLACEHOLDER: long-run mean APY (single-asset, fat tails + crash correlation)",
      "p_losing_month": "TODO_PLACEHOLDER: target <~0.15",
      "loss_1in100": "TODO_PLACEHOLDER: 1-in-100 monthly loss as frac of capital, target <~0.10",
      "worst_month": "TODO_PLACEHOLDER: worst-month drawdown",
      "disclosure_text": "You earn the volatility risk premium in calm markets and absorb losses in crashes. In FULL the pool cannot become insolvent and cannot be run, but YOUR CAPITAL IS NOT GUARANTEED.",
      "two_separate_claims": [
        "(1) LPs are always paid — no bad debt, FULL, code-enforced (I1).",
        "(2) Depositors can lose principal in a crash — capital NOT guaranteed."
      ]
    },

    "parameter_provenance": {
      "_doc": "mirror the existing params.json provenance map for the cvAMM block; every field must be calibrated/derived, never guessed",
      "fair_premium_surface": "exact closed form (Phi-sum), verified vs il.py to ~5e-11 — NO calibration needed (sigma_ref is the only input)",
      "sigma_ref": "TODO: calibrated (EWMA windows + floor from historical backtest)",
      "base_load_by_regime": "TODO: calibrated (lone-writer vs diversified CVaR envelope)",
      "util_skew": "TODO: calibrated",
      "dispersion_skew": "TODO: calibrated",
      "max_load_bps": "TODO: calibrated",
      "productive_collateral": "TODO: roadmap",
      "pool_hedge_fraction": "TODO: roadmap",
      "tranche_cut": "TODO: roadmap",
      "disclosure": "TODO: calibrated (single-asset depositor loss distribution)"
    }
  }
}
